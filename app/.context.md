<context path="app" updated="2026-09-17">

Application manifests directory containing 22 catalog items that can be installed via Infinite OS. Each subdirectory represents a deployable application with a manifest (installation instructions, dependencies, metadata) and branding assets. `supabase/` is a placeholder without a manifest yet.

## Summary

- adminer/ — Lightweight database management tool (PHP-based)
- adobeCommerce/ — Adobe Commerce (e-commerce platform)
- drupal/ — Open-source CMS platform
- evolution-api/ — WhatsApp automation API
- hermes-agent/ — Hermes AI agent by Nous Research (Python service)
- hermes-dashboard/ — Web dashboard companion for the Hermes agent
- joomla/ — Content management system
- mautic/ — Marketing automation platform
- moodle/ — Learning management system
- n8n/ — Workflow automation platform
- opencart/ — Open-source e-commerce platform
- openmage/ — Magento 1 fork (e-commerce)
- open-webui/ — Self-hosted LLM chat interface
- passbolt/ — Open-source password manager
- phpmyadmin/ — MySQL/MariaDB web administration tool
- pocketbase/ — Backend-in-a-box (SQLite, auth, REST API, admin UI)
- privatebin/ — Zero-knowledge paste bin
- supabase/ — Firebase alternative (placeholder, no manifest yet)
- typebot/ — Chatbot builder
- umami/ — Privacy-focused web analytics
- uptime-kuma/ — Self-hosted monitoring tool
- wordpress/ — Web content management system

## Constraints

- Each application subdirectory MUST contain a manifest file (`.json`, `.yml`, or `.yaml`).
- Each application subdirectory MUST contain an `assets/` directory with at minimum an avatar image.
- Manifest `type` property MUST be set to `app`.

## Guidance

- Application manifests SHOULD specify required services in the `services` property (e.g., `php:8.2`, `mariadb`).
- Applications requiring user configuration SHOULD define `dataFields` for installation-time parameters.
- Python-based apps (`hermes-agent`, `hermes-dashboard`, `open-webui`) install via `mise x python@3.11 -- pip install <pinned-version>`.

</context>
