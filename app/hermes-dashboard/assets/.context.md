<context path="app/hermes-dashboard/assets" updated="2026-09-17">

Marketplace image and startup script for the Hermes dashboard. The manifest copies the script to `/app/hermes-agent/` and uses it as the service start command.

## Summary

- avatar.png — Application icon
- start_hermes_dashboard.sh — Startup script for the dashboard service

## Constraints

- avatar.png path MUST match the `avatarUrl` in ../manifest.yaml.

</context>
