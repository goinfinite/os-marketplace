<context path="app/moodle/assets" updated="2026-09-17">

Marketplace images and the .htaccess file for Moodle. The manifest copies `.htaccess` into the install directory before deploying Moodle files.

## Summary

- .htaccess — Apache access rules copied to the install directory
- avatar.png — Application icon (720x720)
- screenshot-1.png through screenshot-3.png — UI screenshots for marketplace display

## Constraints

- Image file paths MUST match the `avatarUrl` and `screenshotUrls` in ../manifest.yml.

</context>
