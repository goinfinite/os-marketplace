<context path="app/n8n/assets" updated="2026-09-17">

Marketplace images and the environment template for n8n. The manifest copies `env.txt` into the install as `.env` and replaces its placeholders.

## Summary

- avatar.jpg — Application icon (720x720)
- env.txt — Environment template; placeholders `n8n_base_url` and `n8n_user_folder` are substituted at install
- screenshot-1.jpg through screenshot-3.jpg — UI screenshots for marketplace display

## Constraints

- Image file paths MUST match the `avatarUrl` and `screenshotUrls` in ../manifest.yml.

</context>
