<context path="app/privatebin/assets" updated="2026-09-17">

Marketplace images and the configuration file for PrivateBin. The manifest copies `conf.php` to the install's `cfg/conf.php` and substitutes the hostname.

## Summary

- avatar.jpg — Application icon (720x720)
- conf.php — PrivateBin configuration with a `goinfinite.local` hostname placeholder
- screenshot-0.png through screenshot-4.png — UI screenshots for marketplace display

## Constraints

- Image file paths MUST match the `avatarUrl` and `screenshotUrls` in ../manifest.yml.

</context>
