ADMIN_EMAIL=admin_email

# Make sure to change this to your own random string of 32 characters
# https://docs.typebot.io/self-hosting/deploy/docker#2-add-the-required-configuration
ENCRYPTION_SECRET=encryption_secret

# Database URL
DATABASE_URL=postgresql://pg_user:pg_pass@127.0.0.1:5432/pg_db?schema=typebot

# NextAuth URLs
NEXTAUTH_URL=nextauth_url
NEXT_PUBLIC_VIEWER_URL=next_public_viewer_url

# SMTP Configuration for Email Authentication
SMTP_HOST=smtp_host
SMTP_PORT=smtp_port
SMTP_USERNAME=smtp_username
SMTP_PASSWORD=smtp_password
SMTP_AUTH_DISABLED=false
NEXT_PUBLIC_SMTP_FROM=Typebot <smtp_from>

# For more configuration options check out: https://docs.typebot.io/self-hosting/configuration
