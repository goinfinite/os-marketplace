<context path="docs" updated="2026-09-17">

Project documentation directory. Holds the feature map that indexes every user-facing marketplace feature to its implementing paths in this repository.

## Summary

- FEATURE-MAP.md — Feature index: each entry lists the file path a feature travels, from manifest entry point to output

## Constraints

- Every feature entry MUST trace a real path through files in this repository; the map executor (Infinite OS) is external and is named as such.

</context>
